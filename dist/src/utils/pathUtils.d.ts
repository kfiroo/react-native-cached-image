declare namespace _default {
    /**
     * Given a URL and some options returns the file path in the file system corresponding to it's cached image location
     * @param url
     * @param cacheLocation
     * @returns {string}
     */
    function getImageFilePath(url: any, cacheLocation: any): string;
    /**
     * Given a URL and some options returns the file path in the file system corresponding to it's cached image location
     * @param url
     * @param cacheLocation
     * @returns {string}
     */
    function getImageFilePath(url: any, cacheLocation: any): string;
    /**
     * Given a URL returns the relative file path combined from host and url hash
     * @param url
     * @returns {string}
     */
    function getImageRelativeFilePath(url: any): string;
    /**
     * Given a URL returns the relative file path combined from host and url hash
     * @param url
     * @returns {string}
     */
    function getImageRelativeFilePath(url: any): string;
    /**
     * returns the url after removing all unused query params
     * @param url
     * @param useQueryParamsInCacheKey
     * @returns {string}
     */
    function getCacheableUrl(url: any, useQueryParamsInCacheKey: any): string;
    /**
     * returns the url after removing all unused query params
     * @param url
     * @param useQueryParamsInCacheKey
     * @returns {string}
     */
    function getCacheableUrl(url: any, useQueryParamsInCacheKey: any): string;
}
export default _default;
